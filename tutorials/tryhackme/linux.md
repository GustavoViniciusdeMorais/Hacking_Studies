# Linux
```bash
su -l user2
scp /tmp/index.php steve@stapp02:/home/web
python3 -m http.server 8080 --bind 0.0.0.0
ip -c a
# Review apt repositories and gpg key
add-apt-repository []
# add gpg key
wget -qO - url/pug.gpg | sudo apt-key add -
# add source list
cd /etc/apt/sources.list.d && touch new.list && nano new.list
# deb http://url apt/stable/
apt update
apt install new
```
